// Copy to Local.xcconfig and set your own Apple Developer team id
// (Xcode → Settings → Accounts, or `security find-identity -v -p codesigning`).
// Only needed to run on a physical device; the simulator does not sign.
DEVELOPMENT_TEAM = YOURTEAMID
CODE_SIGN_STYLE = Automatic
